<div class="col-12 col-md-4 p-0">
  <%= link_to team_employee_path(@team, employee) do %>
    <div class="employee-card">
        <div class="p-0">
          <h5><%= employee.first_name %> <%= employee.last_name %></h5>
          <h6><%= employee.role %></h6>
      </div>
    </div>
  <% end %>
</div>
